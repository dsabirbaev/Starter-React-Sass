

import "./style.scss";

const Card = (props) => {
    const {text, img, desc, isBuy} = props.state;
    return (
        <div className="card">
            {/* Card */}
        </div>
    );
};

export default Card;